'use client';
import {FormEvent,useState} from 'react';
import {Wrench,ArrowRight} from 'lucide-react';
import Link from 'next/link';
import {useRouter} from 'next/navigation';
export default function TechnicianLogin(){const router=useRouter();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');const r=await fetch('/api/technician/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(r.ok)router.replace('/technician');else setError(d.error||'Login failed.');setBusy(false)}return <section className="section"><div className="container narrow"><div className="auth-card form-card"><div className="logo-mark"><Wrench size={18}/></div><div className="eyebrow">TECHNICIAN PORTAL</div><h1>Technician Login</h1><p className="muted">Injira ukoresheje email wiyandikishijeho na password wahawe na Admin.</p>{error&&<p className="form-error">{error}</p>}<form onSubmit={submit}><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></div><button className="btn btn-green" disabled={busy}>{busy?'Injira...':'Login'} <ArrowRight size={16}/></button></form><p className="auth-footer">Ntabwo uri Technician? <Link className="text-link" href="/technician/apply">Saba access</Link></p></div></div></section>}
