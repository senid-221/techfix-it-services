import {notFound} from 'next/navigation';import {services} from '@/lib/services';import {catalogServices} from '@/lib/service-catalog';import ServiceDetailClient from './ServiceDetailClient';
export function generateStaticParams(){return Array.from(new Set([...services.map(service=>service.slug),...catalogServices.map(service=>service.slug)])).map(slug=>({slug}));}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const exists=services.some(item=>item.slug===slug)||catalogServices.some(item=>item.slug===slug);if(!exists)notFound();return <ServiceDetailClient slug={slug}/>}
