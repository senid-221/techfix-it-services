import {NextResponse} from 'next/server';import {isAdminSession} from '@/lib/admin-auth';import {getSupabaseAdmin} from '@/lib/supabase';
const transitions:Record<string,string[]>={PENDING:['APPROVED','REJECTED'],APPROVED:['IN_PROGRESS'],IN_PROGRESS:['COMPLETED']};
function shapeHistory(rows:any[]){return rows.map(h=>({id:h.id,from_status:h.old_status??null,to_status:h.new_status,note:h.note??null,changed_by:h.changed_by??null,created_at:h.created_at}))}
async function enrich(db:any,data:any){const[{data:service},{data:tech},{data:media}]=await Promise.all([db.from('services').select('id,name,slug,description,active,category_name,price_type,min_price,max_price,currency,estimated_duration_minutes,booking_methods,image').eq('id',data.service_id).maybeSingle(),data.technician_id?db.from('technicians').select('id,name,full_name,phone,email,active').eq('id',data.technician_id).maybeSingle():Promise.resolve({data:null}),db.from('booking_media').select('id,file_name,mime_type,size_bytes,storage_path,created_at').eq('booking_id',data.id).order('created_at',{ascending:true})]);const signedMedia=await Promise.all((media||[]).map(async(m:any)=>{const signed=await db.storage.from('booking-evidence').createSignedUrl(m.storage_path,900);return{id:m.id,file_name:m.file_name,mime_type:m.mime_type,size_bytes:m.size_bytes,created_at:m.created_at,url:signed.data?.signedUrl||null}}));const technician=tech?{...tech,name:tech.name||tech.full_name||'Technician'}:null;return{...data,service_name:service?.name||'IT Service',service_slug:service?.slug||null,service_category:service?.category_name||null,price_type:service?.price_type||'QUOTE',min_price:service?.min_price??null,max_price:service?.max_price??null,currency:service?.currency||'RWF',estimated_duration_minutes:service?.estimated_duration_minutes??null,booking_methods:service?.booking_methods||[],service_image:service?.image||null,technicians:technician,media:signedMedia}}
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){if(!(await isAdminSession()))return NextResponse.json({error:'Unauthorized'},{status:401});const{id}=await params;try{const db=getSupabaseAdmin();const{data,error}=await db.from('bookings').select('id,booking_number,service_id,technician_id,customer_name,customer_phone,preferred_date,preferred_time,notes,admin_note,problem_description,service_method,province,district,sector,cell,address,google_maps_url,device_type,problem_started,powers_on,error_message,diagnosis_unknown,status,tracking_token,created_at,updated_at').eq('id',id).maybeSingle();if(error)return NextResponse.json({error:error.message},{status:500});if(!data)return NextResponse.json({error:'Booking not found.'},{status:404});const{data:history,error:historyError}=await db.from('booking_status_history').select('id,old_status,new_status,note,changed_by,created_at').eq('booking_id',id).order('created_at',{ascending:true});if(historyError)return NextResponse.json({error:historyError.message},{status:500});return NextResponse.json({booking:await enrich(db,data),history:shapeHistory(history||[])})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to load booking.'},{status:500})}}
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){if(!(await isAdminSession()))return NextResponse.json({error:'Unauthorized'},{status:401});const{id}=await params;try{const body=await request.json();const db=getSupabaseAdmin();const{data:current,error:ce}=await db.from('bookings').select('id,status,technician_id,admin_note').eq('id',id).single();if(ce||!current)return NextResponse.json({error:'Booking not found.'},{status:404});if(body.technicianId!==undefined){const technicianId=body.technicianId===null||body.technicianId===''?null:Number(body.technicianId);if(technicianId!==null&&!Number.isFinite(technicianId))return NextResponse.json({error:'Invalid technician.'},{status:400});if(technicianId!==null){const{data:tech,error:te}=await db.from('technicians').select('id').eq('id',technicianId).eq('active',true).maybeSingle();if(te||!tech)return NextResponse.json({error:'Selected technician is unavailable.'},{status:400})}const{data,error}=await db.from('bookings').update({technician_id:technicianId,admin_note:typeof body.note==='string'?body.note.trim().slice(0,1000):current.admin_note}).eq('id',id).select('id,booking_number,service_id,technician_id,customer_name,customer_phone,preferred_date,preferred_time,notes,admin_note,problem_description,service_method,province,district,sector,cell,address,google_maps_url,device_type,problem_started,powers_on,error_message,diagnosis_unknown,status,tracking_token,created_at,updated_at').single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({booking:await enrich(db,data)})}
const next=typeof body.status==='string'?body.status:'';if(!['APPROVED','REJECTED','IN_PROGRESS','COMPLETED'].includes(next))return NextResponse.json({error:'Invalid status.'},{status:400});if(!transitions[current.status]?.includes(next))return NextResponse.json({error:`Invalid transition from ${current.status} to ${next}.`},{status:409});const patch:Record<string,unknown>={status:next};if(typeof body.note==='string')patch.admin_note=body.note.trim().slice(0,1000);const{data,error}=await db.from('bookings').update(patch).eq('id',id).select('id,booking_number,service_id,technician_id,customer_name,customer_phone,preferred_date,preferred_time,notes,admin_note,problem_description,service_method,province,district,sector,cell,address,google_maps_url,device_type,problem_started,powers_on,error_message,diagnosis_unknown,status,tracking_token,created_at,updated_at').single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({booking:await enrich(db,data)})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to update booking.'},{status:500})}}
