import {NextResponse} from 'next/server';import {createSessionToken,adminCookieName} from '@/lib/admin-auth';export async function POST(request:Request){try{const{email,password}=await request.json();if(email!==process.env.TECHFIX_ADMIN_EMAIL||password!==process.env.TECHFIX_ADMIN_PASSWORD)return NextResponse.json({error:'Invalid admin credentials.'},{status:401});const r=NextResponse.json({ok:true});r.cookies.set(adminCookieName,createSessionToken(),{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',maxAge:43200});return r}catch{return NextResponse.json({error:'Invalid request.'},{status:400})}}
