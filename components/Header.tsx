'use client';
import Link from 'next/link';import {Menu,X,Search,Wrench} from 'lucide-react';import {useState} from 'react';import {Button} from './Button';
export function Header(){const[open,setOpen]=useState(false);const close=()=>setOpen(false);return <header className="site-header"><div className="container nav-wrap"><Link href="/" className="logo" onClick={close}><span className="logo-mark">T</span><span>Tech<span>Fix</span></span></Link><nav className={open?'nav open':'nav'}><Link href="/#services" onClick={close}>Services</Link><Link href="/#how-it-works" onClick={close}>How it works</Link><Link href="/track" onClick={close}><Search size={15}/> Track booking</Link><Link href="/technician/login" onClick={close}><Wrench size={15}/> Technician</Link><Link href="/#why-us" onClick={close}>Why us</Link><Link href="/#contact" onClick={close}>Contact</Link><Button href="/book">Book a Service</Button></nav><button className="menu-btn" aria-label={open?'Close menu':'Open menu'} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></header>}
